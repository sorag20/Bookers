<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>BLOG</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <%- include('header');%>
    <div class="sign">
      <div class="container">
        <div class="panel">
          <h2>本の登録</h2>
          <% if (errors.length > 0) { %>
            <ul class="errors">
              <% errors.forEach(error => { %>
                <li><%= error %></li>
              <% }); %>
            </ul>
          <% } %>
          <form action="/register" method="post">
            <p>本の題名</p>
            <input type="text" name="name">
            <p>総ページ数</p>
            <input type="text" name="total_pages">
            <p>読み進めたページ数</p>
            <input type="text" name="finished_pages">
            <input type="submit" value="登録する">
            <a href="/list">一覧にもどる</a>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>